import {Stack, Typography} from "@mui/material";
import React from "react";

const Phrases = () => {
    return (
        <Stack direction={"column"} sx={{fontFamily:"Agro",color:"#34515e"}}>
            <Typography variant={"h1"}>J-LINE Make Up</Typography>
            <Typography variant={"h1"}>with professional</Typography>
            <Typography variant={"h1"}>In Gimpo</Typography>
        </Stack>
    );
};
export default Phrases;