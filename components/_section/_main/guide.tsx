import {Stack, Typography} from "@mui/material";
import React from "react";

const Guide = () => {


    return (
        <Stack flexDirection={"column"} justifyContent={"space-between"}>
            <Typography variant={"h1"} sx={{fontFamily:"Agro"}}>제이라인</Typography>
        </Stack>
    );
};
export default Guide;